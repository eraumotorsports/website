<div class="page-header">
  <h1 id="title" contenteditable="true"><%= raw @page.title %></h1>
</div>

<div class="content">
  <div id="content" contenteditable="true">
    <%= raw @page.content %>
  </div>
</div>

<a id="save" href="#" onclick="saveContent();">Save</a>
